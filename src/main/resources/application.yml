server:
  port: 8080

spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/mini-gestor
    username: postgres
    password: DYEGORAFA7463521#
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
